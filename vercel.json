{
    "builds":[{
        "src": "OnlineWork/wsgi.py",
        "use":"@vercel/pyhton",
        "config": { "maxlambdaSize": "15mb" , "runtime" : "python3.9"}
    }],
    "routes":[
        {
            "src": "/(.*)",
            "dest":"OnlineWork/wsgi.py"
        }
    ]
}